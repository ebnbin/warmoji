{
  "name": "warmoji",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/check-defs.ts && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "bitecs": "^0.4.0",
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^6.0.3",
    "vite": "^8.1.4"
  }
}
