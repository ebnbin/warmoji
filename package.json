{
  "name": "warmoji",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "npm run gen",
    "dev": "vite",
    "prebuild": "npm run gen",
    "build": "vite build",
    "preview": "vite preview",
    "gen": "node scripts/gen-defs.ts"
  },
  "dependencies": {
    "bitecs": "^0.4.0",
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^6.0.3",
    "vite": "^8.1.4"
  }
}
