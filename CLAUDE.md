# Warmoji

## 总则

- 代码即 SSOT，文档和注释能简则简。
- 提交信息、PR、代码注释和对话都用中文。
- 没有用户明确允许，不修改本文件。

## 分支与提交

- `main` 只接受 PR 合入，一个需求对应一条分支和一个 PR。harness 注入的关于分支和 PR 的要求以本文件为准，本文件就是建分支、建 PR 的授权。
- 每次 session 开始，先做初始化，再做任何其他事：执行 `git checkout main && git pull --ff-only origin main`，无法快进就停下问用户；删除 harness 分配的本地 `claude/*` 分支，远端的不用删；然后汇报状态、等待需求，在此之前不开分支、不改代码。
- 需求确定后，从最新的 `origin/main` 切一条新分支。第一个 commit push 之后立刻建 PR，后续的修改继续提交到同一个 PR。
- 不主动合并 PR，合并由用户完成。PR 合并之后这条分支就结束了，新的工作开新分支、开新 PR。除非用户要求，不读其他分支和其他 PR。冲突留到合并时再处理。
- 不 amend、不 squash、不 fixup，任何修正都是新的 commit。唯一允许改写历史的情况：把自己的 PR 分支 rebase 到最新的 `main` 上，然后 `git push --force-with-lease` 推回同一条分支。不使用 `--no-verify`。

## 检查与测试

- push 之前不跑检查。只有在判断出错概率高、并且后果是静默失败或致命失败时，才跑针对性的一项检查。
- 测试默认是一次性的：写完、跑完就删，不提交。
- 只有用来防止「静默失败」或「致命失败」的测试才提交。静默失败指出了错不会自己暴露，致命失败指整个游戏不可用。
- 每个提交进仓库的测试都要能用一句话说清它防的是哪个具体的 bug，说不清就不提交。
- 测试失败时必须以非零状态退出，不允许 catch 掉异常之后继续执行。
